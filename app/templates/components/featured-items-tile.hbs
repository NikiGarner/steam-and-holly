<div class="row">
  <img src="{{item.image}}" alt="" />
</div>
<div class="row">
  {{#link-to 'item' item.id}}{{item.name}}{{/link-to}}
  {{item.cost}}
  <button class="btn btn-sm btn-primary" {{action 'addToCart' item}}>Add to Cart</button>
</div>
